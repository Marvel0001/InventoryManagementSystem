<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ism.dao.AdminMapper">
    <resultMap id="Admin" type="com.ism.domain.Admin">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="sex" column="sex"/>
        <result property="telephone" column="telephone"/>
        <result property="address" column="address"/>
        <result property="storehouseId" column="storehouseId"/>
        <result property="role" column="role"/>
    </resultMap>
    <insert id="addAdmin">
        insert into
            Admin (name, sex, telephone, address, storehouseId, role)
        VALUES
            (#{name}, #{sex}, #{telephone}, #{address}, #{storehouseId}, #{role})
    </insert>
    <update id="modifyAdmin">
        update Admin set name=#{name}, sex=#{sex}, telephone=#{telephone}, address=#{address}, storehouseId=#{storehouseId}, role=#{role}
        where id=#{id}
    </update>
    <delete id="deleteAdmin">
        delete from Admin where id=#{id}
    </delete>
    <select id="selectAdminById" resultType="com.ism.domain.Admin">
        select * from Admin where id=#{adminId}
    </select>
    <select id="selectAllAdmin" resultType="com.ism.domain.Admin">
        select * from Admin
    </select>
    <select id="selectAdminByName" resultType="com.ism.domain.Admin">
        select * from Admin where name LIKE concat('%', concat(#{name}, '%'))
    </select>
    <select id="selectAdminByStorehouseId" resultType="com.ism.domain.Admin">
        select * from Admin where storehouseId=#{storehouseId}
    </select>


</mapper>